# BoringSSL requires go and perl, we don't support it now.

# if (NOT CRYPTO_USE_BORINGSSL) set(CRYPTO_USE_BORINGSSL TRUE CACHE BOOL "Cache ssl selector and directly use boringssl next time") endif()
